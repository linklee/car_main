<%= form_for(@ba) do |f| %>
  <% if @ba.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ba.errors.count, "error") %> prohibited this ba from being saved:</h2>

      <ul>
      <% @ba.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :src %><br>
    <%= f.text_field :src %>
  </div>
  <div class="field">
    <%= f.label :descr %><br>
    <%= f.text_field :descr %>
  </div>
  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
