<% provide(:title, 'All leads') %>

<h1>All customers</h1>
 <% flash.each do |key, value| %>
  <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
  
<%= will_paginate %>

<ul class="leads">
  <%= render @leads %>
</ul>

<%= will_paginate %>