<center>
<%= form_for(@page_part) do |f| %>
  <% if @page_part.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@page_part.errors.count, "error") %> prohibited this page_part from being saved:</h2>

      <ul>
      <% @page_part.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :page_name %><br>
    <%= f.text_field :page_name %>
  </div>
  <div class="field">
    <%= f.label :text %>
    <%= f.text_area :text, :style => "min-width:800px;min-height:600px" %>
  </div><br/>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
</center>